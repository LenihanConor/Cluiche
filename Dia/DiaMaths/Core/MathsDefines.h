#pragma once

namespace Dia
{
	namespace Maths
	{
		static const float PI   = 3.1415926535897932384626433832795f;
		static const float PI_2	= (PI * 2.0f);
		static const float PI_HALF	= (PI / 2.0f);
		static const float PI_ONE_OVER = (1.0f / PI);
		static const float PI_HALF_ONE_OVER = (2.0f / PI);

		static const float DEGREETORADII = 0.0174532925199f;
		static const float RADIITODEGREE = 57.295779513082f;

		static const float MAX_RADINS = PI_2;
		static const float MIN_RADIANS = 0;

		static const float ONE_OVER_SQRT_2	= 0.70710678118654746f;

		static const float FLOAT_EPSILON = 0.0001f;
	}
}