**Spec:** @docs/specs/systems/cluicheeditor/diaeditorapi.md
**Status:** In Progress

# Phase 1 — Automation Testing

| # | Task | Test | Status | Model | Notes |
|---|------|------|--------|-------|-------|
| 1 | Scaffold `DiaEditorAPI` module — create directory under `Dia/DiaEditor/`, add `EditorActionDescriptor`, `EditorActionParamSchema`, `EditorActionManifest`, `DispatchThread` enum, `ActionHandler` typedef. Add to `DiaEditor.vcxproj`. | Build passes; `dia check deps` clean | Todo | sonnet | New module, no existing code to migrate |
| 2 | Implement `EditorActionRegistry` — `RegisterAction()`, `DeregisterActionsForOwner()`, `GetManifest()`, `ExecuteAction()`. Cross-register with `DiaAPI::RegisterCommandJson()` on every `RegisterAction()` call. Handler stored once; both routes call `ExecuteAction()`. Add `DIA_LOG_INFO` on register/deregister, `DIA_LOG_WARN` on duplicate name, `DIA_LOG_ERROR` on unknown action in `ExecuteAction()`. Register `actions_registered` counter and `actions_deregistered` counter with `MetricRegistry`. | Unit: register succeeds; duplicate returns false + logs warn; execute unknown returns error JSON + logs error; deregister by owner removes all owned actions; metric counters increment correctly | Todo | sonnet | EAPI-001, EAPI-006 |
| 3 | Implement `EditorActionQueue` + `EditorActionModule` — thread-safe push queue, `DoUpdate()` drains and executes `kMainThread` actions, sets `std::promise` result. Python caller blocks on `std::future::get()` with 5s timeout. Add `DIA_TRACE_ZONE` in `DoUpdate()`, `DIA_LOG_WARN` on timeout, `DIA_LOG_ERROR` on handler exception. Register `queue_depth` gauge and `action_dispatch_ms` histogram with `MetricRegistry`. Register module in `editor.diaapp`. | Unit: push/drain roundtrip returns correct result; timeout fires after 5s and logs warn; concurrent pushes from 2 threads both complete; queue_depth gauge reflects pending count | Todo | sonnet | EAPI-004; module added to `editor.diaapp` manifest |
| 4 | Migrate P0 project actions — dual-register `project.open_path`, `project.close`, `project.get_state` by wrapping existing `ProjectContextController` handlers behind `RegisterAction()` calls. JS path still routes through `WebUIBridge → ExecuteAction()`. | (a) Editor opens/closes project via JS UI — no regression; (b) same 3 actions callable via `DiaAPI::ExecuteCommandJson` and return valid JSON; (c) log output shows single handler invocation per call (no double-dispatch) | Todo | sonnet | EAPI-005; no handler duplication |
| 5 | Migrate P0 game_connection actions — dual-register `game_connection.connect`, `game_connection.disconnect`, `game_connection.get_state` from `GameConnectionController`. | (a) Game connects/disconnects via JS UI — no regression; (b) `game_connection.get_state` callable via script returns valid JSON; (c) `DIA_LOG_INFO` emitted on connect/disconnect state transitions | Todo | sonnet | Same pattern as task 4 |
| 6 | Migrate P0 plugin actions — `plugin.load`, `plugin.unload` re-registered via `DiaEditorAPI::RegisterAction()` so they appear in the manifest and honour `EditorActionQueue` dispatch. Add `DIA_LOG_INFO` on load/unload. | `plugin.load` callable from Python returns success JSON and appears in `GetManifest()` output; `plugin.unload` removes owner's actions from manifest and logs deregistration | Todo | sonnet | Already script-reachable; task surfaces them in manifest and adds observability |
| 7 | Implement `GeneratePythonModule()` — enumerate manifest, create per-category sub-modules (`dia_editor.project`, `dia_editor.game_connection`, etc.) via `DiaPython::CreateModule`/`AddFunction`. Each callable validates params, calls `ExecuteAction`, returns dict. Call after `PluginLoaderModule` completes. Add `DIA_LOG_INFO` listing module name + action count on generation. Add `DIA_LOG_ERROR` if param validation fails at call time. | (a) `import dia_editor; dia_editor.project.get_state()` returns valid dict; (b) calling with a missing required param raises Python `ValueError` with clear message; (c) log shows each sub-module generated with correct action count | Todo | sonnet | EAPI-002; startup ordering: fires after all plugins loaded |
| 8 | Implement `EmitPythonStubs()` — generate `dia_editor.pyi` from manifest descriptors, write to `Cluiche/out/CluicheEditor/scripts/`. Call alongside `GeneratePythonModule()`. | (a) `.pyi` file present after editor startup; (b) contains correct type hints for all registered actions; (c) `mypy scripts/smoke_test.py` reports no errors against the stub | Todo | haiku | PD-009 |
| 9 | Implement `EditorAPIHealthReporter` — `IHealthReporter` subclass reporting `Healthy` when registry has ≥1 action and `EditorActionModule` is draining; `Degraded` if queue depth exceeds 20 for >2 consecutive frames; `Unhealthy` if `GeneratePythonModule()` was never called. Register with `HealthRegistry`. | Unit: healthy when registry populated + draining; degraded when queue backed up; unhealthy when Python not generated | Todo | sonnet | Observability pillar: Health |
| 10 | End-to-end smoke test script — write `scripts/smoke_test.py`: (a) `dia_editor.project.get_state()` — assert returns dict with `state` key; (b) `dia_editor.game_connection.get_state()` — assert returns dict with `connected` key; (c) call an unknown action name via raw `ExecuteCommandJson` — assert error JSON returned, no crash. Run against live editor. | All 3 assertions pass; log output shows expected `DIA_LOG_INFO` entries; no `DIA_LOG_ERROR` in output | Todo | haiku | Phase 1 acceptance gate |

# Validation Checkpoints

| After task | Check |
|------------|-------|
| 3 | `dia run googletest --filter="EditorAction*"` — all unit tests green |
| 6 | Manual: open editor, open project via UI, call `project.get_state` via DiaAPI console — same result both ways |
| 8 | `mypy scripts/smoke_test.py` clean against generated `.pyi` |
| 10 | Full smoke script passes; `DIA_LOG_ERROR` count = 0 in run output |

# Phase 2 — AI Tooling (deferred)

| # | Task | Test | Status | Model | Notes |
|---|------|------|--------|-------|-------|
| 11 | Protocol-agnostic manifest serialiser interface — `IManifestSerialiser` with `Serialise(const EditorActionManifest&)`. Add `DIA_LOG_INFO` on serialisation start/complete. | Unit: serialiser produces non-empty output for a 3-action manifest | Deferred | sonnet | EAPI-003 |
| 12 | MCP adapter — `tools/list` + `tools/call` over DiaWebSocket, fixed port 7777 (EAPI-007). Add `DIA_LOG_INFO` on bind/accept/call. Register `mcp_calls_total` counter. `DIA_LOG_WARN` if port already in use (clear error, not crash). | `curl localhost:7777/tools/list` returns JSON array of all registered actions; `tools/call project.get_state` returns valid result | Deferred | sonnet | Requires DiaWebSocket |
| 13 | Migrate P1 pipeline actions (`pipeline.build`, `pipeline.get_status`) | Same dual-registration pattern as tasks 4–6 | Deferred | sonnet | DiaPipelineEditor WebUIBridge |
| 14 | Ollama at-desk workflow validation — natural language → `tools/list` → param gather → `tools/call` → editor acts | "Get the project state" → Ollama calls `project.get_state` → correct result returned to user | Deferred | opus | End-to-end with local Ollama instance |
