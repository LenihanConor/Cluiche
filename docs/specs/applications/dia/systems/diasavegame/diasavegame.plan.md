**Spec:** @docs/specs/applications/dia/systems/diasavegame/diasavegame.md
**Status:** In Progress

## Tasks

| # | Task | Test | Status | Model | Notes |
|---|------|------|--------|-------|-------|
| 1 | Project scaffold — `DiaSaveGame/` directory; `DiaSaveGame.vcxproj` + `.filters`; register in `Cluiche.sln` under `domain/gameplay/core`; `dia.savegame.architecture.module.md` | `dia check deps` clean | Done | haiku | `dia check deps` clean (25 pre-existing unrelated issues, 0 from DiaSaveGame) |
| 2 | `ISaveable` + `SaveRegistry` — `ISaveable.h` (Serialize/Deserialize/GetVersion); `SaveRegistry.h/.cpp` (Register/Unregister/RegisterMigration, DynamicArrayC participant list, HashTable lookup by StringCRC); registration order preserved | GoogleTest: register 3 participants, enumerate in order; unregister middle; re-register at end | Done | sonnet | ISaveable.h, SaveRegistry.h/.cpp created; DynamicArrayC<ParticipantEntry,32> + DynamicArrayC<MigrationEntry,64>; DIA_ASSERT guards on double-reg and overflow |
| 3 | `SaveConfig` + slot management — `SaveConfig.h` struct; `SlotInfo.h`; `SlotManager.h/.cpp` (EnumerateSlots, DeleteSlot, RenameSlot, path construction from slotPattern, max-slot cap enforcement) | GoogleTest: slot name generation from pattern; enumeration returns all on-disk slots; delete removes file; max-slot cap rejects overflow | Done | sonnet | SaveFormat.h, SaveConfig.h, SlotInfo.h, SlotManager.h/.cpp created; Win32 FindFirstFileA/DeleteFileA/MoveFileA; BuildPath replaces {id} placeholder via snprintf |
| 4 | `SaveContext` / `LoadContext` — `SaveContext.h/.cpp` (Write overloads for int32, float, bool, const char*; BeginObject/EndObject/BeginArray/EndArray; in-memory buffer accumulation); `LoadContext.h/.cpp` (Read overloads, BeginObject/EndObject/BeginArray+count/EndArray; backed by DiaSerializer primitives); format tag selects JSON vs binary backend | GoogleTest: round-trip int, float, bool, string; nested object; array of ints; missing key returns false | Done | sonnet | SaveContext.h/.cpp + LoadContext.h/.cpp with jsoncpp node stack; 8 tests pass (round-trip int/float/bool/string, nested object, array, missing key, wrong type) |
| 5 | `SaveManifest` — `SaveManifest.h/.cpp`; fields: engine version, participant list (id + version), format tag, timestamp; `Write(SaveContext&)` / `Read(LoadContext&)`; compatibility check (engine version mismatch → error; participant version diff → migration flag) | GoogleTest: manifest round-trip; compatibility check rejects wrong engine version; flags participant version delta | Done | sonnet | SaveManifest.h/.cpp: Build() from registry, Write/Read via SaveContext/LoadContext, CheckCompatibility; 3 tests pass (round-trip, engine mismatch, participant delta) |
| 6 | Save / Load orchestration — `SaveManager.h/.cpp`; `Init(SaveConfig, SaveRegistry)`; `Save(slotId)` fires OnSaveStarted → iterates participants in registration order → writes manifest + data; `Load(slotId)` fires OnLoadStarted → reads manifest → validates compatibility → calls Deserialize per participant → fires OnLoadCompleted; `SaveResult` / `LoadResult` `[[nodiscard]]` return types | GoogleTest: 2-participant round-trip (save then load, values restored); load returns error on missing slot; load returns error on engine version mismatch | Done | sonnet | SaveResult.h + SaveManager.h/.cpp: Init/Save/Load/Delete/Rename/Enumerate; manifest write+read+compat; DIA_LOG calls; 3 tests pass (2-participant round-trip, missing slot, engine mismatch) |
| 7 | Versioning + migration — `RegisterMigration(id, fromVersion, fn)` in SaveRegistry; on load, when manifest participant version < current, chain-run migrations in version order before calling Deserialize; `OnMigrationApplied` event fired per step; abort load on missing migration gap | GoogleTest: single migration v1→v2 transforms data correctly; chained v1→v2→v3; missing migration returns error; OnMigrationApplied fires | Done | sonnet | Migration chain loop in SaveManager::Load; FindSavedVersion lambda; HasMigration gap check returns MigrationError; 3 tests pass (single, chained, gap error) |
| 8 | Async I/O — in-memory serialize all participants on sim thread; `std::thread` (or DiaCore job if available) flushes buffer to disk; load reads from disk off-thread then hands buffer back to sim thread for Deserialize; `SaveResult` available synchronously post-flush | GoogleTest: save completes without blocking (mock disk backend); load delivers data to Deserialize on correct thread | Not Started | sonnet | |
| 9 | Observer events — `ObserverSubject` members on `SaveManager` for OnSaveStarted, OnSaveCompleted, OnLoadStarted, OnLoadCompleted, OnMigrationApplied; wire fire points in orchestration and migration paths | GoogleTest: subscribe to all 5 events; run save + load with migration; assert each event fires exactly once with correct fields | Not Started | sonnet | |
| 10 | Test utilities — `DiaSaveGame/Testing/SaveTestHelpers.h`; `InMemorySaveBackend` (bypasses disk, stores buffer in map keyed by slotId); `MockSaveable` (records Serialize/Deserialize call count + written values); `AssertSlotExists`; `AssertEventFired`; add to vcxproj | Used in tasks 2–9 tests | Not Started | haiku | |
