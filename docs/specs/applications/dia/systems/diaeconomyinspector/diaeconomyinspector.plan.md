**Spec:** @docs/specs/applications/dia/systems/diaeconomyinspector/diaeconomyinspector.md
**Status:** In Progress

| # | Task | Test | Status | Model | Notes |
|---|------|------|--------|-------|-------|
| 1 | Editor plugin scaffold — `Dia/DiaEconomyInspector/` vcxproj, module doc, `DiaEconomyInspectorPlugin.h/.cpp`, 4 controller headers+cpps, register in Cluiche.sln under `4.0-Editor` | Build compiles clean | Done | sonnet | 13 files created; build 0 errors. REGISTER_EDITOR_PLUGIN uses `using namespace Dia::EconomyInspector` before macro (same fix needed in DiaAIInspector). |
| 1.5 | Extend DiaEconomy APIs — instance registry on `EconomySystem` (`RegisterInstance`/`UnregisterInstance`/`GetInstanceCount`/`GetInstanceByIndex`); tick rate tracking (`mLastTickIncome`/`mLastTickSpend`) on `ResourcePool` + `GetLastTickIncome()`/`GetLastTickSpend()` on `EconomyInstance`; `GetModifierStack()` on `EconomySystem`; `GetCostTableCount()`/`GetCostTableByIndex()` on `EconomySchema`; `IsDerivedResource()` on `EconomySystem`. Unit tests for all five additions. | `dia run googletest --filter="EconomySystem*:EconomySchema*:EconomyInstance*"` all pass | Done | sonnet | 90/90 pass (71 existing + 19 new). Also fixed 3 pre-existing DynamicArray.inl bugs hit during testing. | These are engine capabilities needed by the inspector sources. All changes confined to DiaEconomy — no game-side touches yet. |
| 2 | `EconomySchemaSource` — one-shot on connect; serialises loaded `EconomySchema` (resource definitions, income rules, cost table) to JSON; topic `"economy.schema"` | Build passes; payload matches spec schema | Done | sonnet | ChangeDetectedSourceBase — hash is stable so broadcasts once per connect. Build 0 errors. |
| 3 | `EconomyInstancesSource` — `ChangeDetectedSourceBase`; iterates all `EconomyInstance` objects via `EconomySystem`; emits pool values, caps, net rate, gross income/spend, per-instance/resource ring buffer (depth 600) sampled once per economy tick; cap saturation and starvation duration computed here | Build passes; payload matches spec schema | Done | sonnet | Tick override advances sample accumulator; UpdateTrackers handles saturation/starvation; oldest-at-head ring. Build 0 errors. |
| 4 | `EconomyModifiersSource` — `ChangeDetectedSourceBase`; emits active modifier stack per instance per resource — type, value, source tag, condition name, active flag | Build passes; payload matches spec schema | Done | sonnet | Fixed ModifierStackEntry namespace (Dia::Economy:: not EconomySystem::). Build 0 errors. |
| 5 | `EconomyEventsSource` — implements `IEconomyObserver`; server-side ring buffer depth 500; full ring sent on initial connect (`full_ring: true`); subsequent pushes send delta (`full_ring: false`) | Build passes; ring replay + delta both covered by tests | Done | sonnet | Custom IInspectorDataSource+IEconomyObserver; new-subscriber detection in Tick drives full-ring replay. Build 0 errors. |
| 6 | React UI — Vite project under `Dia/DiaEconomyInspector/UI/`; four tabs (Resources, Events, Modifiers, Schema); `useBridge` hooks for all four `economy_inspector.*` topics; connection lifecycle (disconnected empty state, connected live data) | `npm run build` clean; Vitest: fill bar, sparkline SVG, event row variants (Earn/Spend/Clamped/Transfer badges, Clamped row highlight, Transfer destination), modifier inactive opacity, derived resource display, schema cost table | Done | sonnet | Match mockup at `@docs/specs/applications/dia/systems/diaeconomyinspector/mock_economy_inspector.html`. Design tokens from DiaEditorUI. Derived resources: purple badge, no fill bar, no income rate. EI-006, EI-008.; 44 Vitest tests (6 suites); build + npm run test clean |
| 7 | C++ unit tests — GoogleTests for ring buffer behavior (depth 500, overflow, full-ring vs delta), saturation/starvation duration logic, change detection hash, JSON payload serialization against all four spec schemas, controller bridge forwarding (stubbed bridge) | `dia run googletest --filter="EconomyInspector*"` all pass | Done | sonnet | Cover EconomyEventsSource ring (depth, overflow, full_ring flag), EconomyInstancesSource saturation/starvation counters, EconomySchemaSource one-shot behavior, all four JSON payloads validated against spec examples.; 38 GoogleTests pass (20 original + 18 from exhaustive audit); EconomyEventsSource final removed for testability |
| 8 | Wire sources into DebugServerHostModule — register all four `economy.*` topics; ensure sources are constructed and destructed with the module | Build passes; `dia run googletest` still passes | Done | sonnet | Follow AIInspectorModule pattern. Sources live in game-side module (SimPU). EventStream or direct IInspectorDataSource registration depending on existing infrastructure.; EconomyInspectorModule (SimPU, Bind/Unbind); build + googletest pass |
| 9 | E2E scenario — `Cluiche/Tests/E2E/scenarios/cluichetest/economy_inspector/test_economy_inspector.py`; navigates to `EconomyTestStage`; subscribes to all four `economy.*` topics via `DiaClient`; asserts payload schemas, ring buffer replay on connect, delta after first event, saturation/starvation badges populated | `dia run e2e --filter economy_inspector` passes | Done | sonnet | Add to `plans/cluichetest/default.json`. Validates game-side sources end-to-end with zero new infrastructure — reuses existing `EconomyTestStage` diastage.; E2E scenario + client wait_for_topic; wired EconomyTestStageModule Bind/Unbind |
