**Spec:** @docs/specs/applications/dia/systems/diaeconomy/diaeconomy.md
**Status:** In Progress

| # | Task | Test | Status | Model | Notes |
|---|------|------|--------|-------|-------|
| 1 | Scaffold — create `Dia/DiaEconomy/` directory, `DiaEconomy.vcxproj`, register in `Cluiche.sln` under `domain/gameplay/core`, add `dia.economy.architecture.module.md` YAML | `dia validate manifest` passes; project builds empty | Pending | haiku | |
| 2 | EconomySchema + ResourceDefinition — `LoadFromJson`, `FindResource`, `GetCost`, `EconomySchemaValidator` (required fields, min ≤ starting ≤ max, unknown field warnings) | Schema loads valid JSON; validator rejects missing fields and incoherent bounds | Pending | sonnet | Uses jsoncpp. `DiaCore/HashTable` for resource lookup. |
| 3 | EconomyInstance — `CreateFromSchema`, `CreateFromJson` (override block), `GetValue` / `GetMaximum` / `GetMinimum` / `GetInstanceName`; pools initialised to `starting_value` (overridden if present) | Instance values match schema defaults; override replaces only the specified starting_value | Pending | sonnet | |
| 4 | Transaction API — `EconomySystem::Earn`, `Spend`, `Transfer`, `SetValue`, `Tick`; `TransactionResult` clamping; income rules fired via `Tick(delta_seconds)`; `[[nodiscard]]` on all result returns | All four operations return correct `TransactionResult`; Tick accumulates income over multiple deltas | Pending | sonnet | Fractional accumulation carried across ticks (ODQ #2 — accumulate, do not truncate) |
| 5 | Observer events — `EconomyObserverSubject`, `IEconomyObserver`; fire `OnPoolChanged`, `OnTransactionClamped`, `OnTransferCompleted`, `OnPoolReachedMaximum`, `OnPoolReachedMinimum` at correct call sites | All five events fire with correct payloads; no spurious double-fires | Pending | sonnet | Min/Max events fire only on transition (not every tick at boundary) |
| 6 | Modifier stack — `multiply_income`, `multiply_cap`, `flat_income` operations; simple modifiers always applied; conditional modifiers gated on `when` field via DiaCondition adaptor (optional compile-time link) | Simple modifier changes effective income; conditional modifier inactive when condition false, active when true | Pending | sonnet | Conditional path is an optional adaptor — builds without DiaCondition if `when` fields absent |
| 7 | Derived resource hook — `RegisterDerivedResource(StringCRC, DerivedResourceFn)` and `QueryDerived` on `EconomySystem` | Registered fn invoked and returns computed float; unregistered name returns 0.0f with DIA_LOG_WARN | Pending | haiku | |
| 8 | Test Utilities — `MockEconomyInstance`, `AssertPoolValue(tolerance)`, `AssertEventFired` under `DiaEconomy/Testing/`; consumer opt-in via include | Utilities compile standalone; no link dependency on GoogleTest | Pending | haiku | |
| 9 | GoogleTest suite — exhaustive coverage: schema load + validation errors, pool clamping at min/max, all TransactionResult variants, observer event payloads, modifier stack (simple + conditional), derived resource registration, income accumulation across variable deltas, Transfer atomicity (deduct then deposit), multi-instance isolation | All tests green via `dia run googletest --filter="DiaEconomy*"` | Pending | sonnet | One test file per feature area. Use test utilities from Task 8. |
| 10 | E2E CluicheTest stage — `EconomyTestStage`: two instances (PlayerEconomy + AIEconomy), tick loop running 10 sim-seconds, Earn/Spend/Transfer transactions, modifier activation mid-run, pytest scenario checkpoints verifying pool values and event counts | `dia run cluichetest` stage completes all checkpoints; pytest scenario passes | Pending | sonnet | Wires `EconomySystem::Tick()` into a SimPU module — exemplar for game code. Console output shows all instance states each second. |
| 11 | Observability pass — `DIA_LOG_INFO` on schema load, instance create, clamped transactions; `DIA_LOG_WARN` on schema validation warnings + unknown derived resource; `DIA_TRACE_ZONE` on `Tick`; `DIA_PROFILE_SCOPE` on modifier evaluation loop; `MetricRegistry` registrations for pool values + per-instance income rate; `IHealthReporter` for instance coherence (min ≤ value ≤ max invariant) | Log lines appear at correct call sites; no missing instrumentation on any code path added in Tasks 2–10 | Pending | sonnet | Full OO scan against all 5 DiaObservation pillars. Run after Task 10 so all code paths exist. |
