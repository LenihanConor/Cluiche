**Spec:** @docs/specs/applications/dia/systems/diaeconomy/diaeconomy.md
**Status:** Done

| # | Task | Test | Status | Model | Notes |
|---|------|------|--------|-------|-------|
| 1 | Scaffold — create `Dia/DiaEconomy/` directory, `DiaEconomy.vcxproj`, register in `Cluiche.sln` under `domain/gameplay/core`, add `dia.economy.architecture.module.md` YAML | `dia validate manifest` passes; project builds empty | Done | haiku | Stub files + vcxproj + sln entry + YAML module doc. Build passes. |
| 2 | EconomySchema + ResourceDefinition — `LoadFromJson`, `FindResource`, `GetCost`, `EconomySchemaValidator` (required fields, min ≤ starting ≤ max, unknown field warnings) | Schema loads valid JSON; validator rejects missing fields and incoherent bounds | Done | sonnet | Uses jsoncpp. DynamicArray for storage. Cost tables parsed as JSON object (spec format). |
| 3 | EconomyInstance — `CreateFromSchema`, `CreateFromJson` (override block), `GetValue` / `GetMaximum` / `GetMinimum` / `GetInstanceName`; pools initialised to `starting_value` (overridden if present) | Instance values match schema defaults; override replaces only the specified starting_value | Done | sonnet | ResourcePool struct with income_accumulator; SetValue_Internal clamped; GetSchema non-owning ptr. |
| 4 | Transaction API — `EconomySystem::Earn`, `Spend`, `Transfer`, `SetValue`, `Tick`; `TransactionResult` clamping; income rules fired via `Tick(delta_seconds)`; `[[nodiscard]]` on all result returns | All four operations return correct `TransactionResult`; Tick accumulates income over multiple deltas | Done | sonnet | Fractional accumulation with floorf carry-over. TODO stubs for Task 5+7. |
| 5 | Observer events — `EconomyObserverSubject`, `IEconomyObserver`; fire `OnPoolChanged`, `OnTransactionClamped`, `OnTransferCompleted`, `OnPoolReachedMaximum`, `OnPoolReachedMinimum` at correct call sites | All five events fire with correct payloads; no spurious double-fires | Done | sonnet | Min/Max transition guard (oldVal < maxVal check). DynamicArrayC<16> subscriber list. |
| 6 | Modifier stack — `multiply_income`, `multiply_cap`, `flat_income` operations; simple modifiers always applied; conditional modifiers gated on `when` field via DiaCondition adaptor (optional compile-time link) | Simple modifier changes effective income; conditional modifier inactive when condition false, active when true | Done | sonnet | IEconomyConditionAdaptor thin interface; Tick has 3 modifier passes; IsModifierConditionMet. |
| 7 | Derived resource hook — `RegisterDerivedResource(StringCRC, DerivedResourceFn)` and `QueryDerived` on `EconomySystem` | Registered fn invoked and returns computed float; unregistered name returns 0.0f with DIA_LOG_WARN | Done | haiku | Raw fn pointer (POD-safe); DynamicArrayC<DerivedEntry,32>. |
| 8 | Test Utilities — `MockEconomyInstance`, `AssertPoolValue(tolerance)`, `AssertEventFired` under `DiaEconomy/Testing/`; consumer opt-in via include | Utilities compile standalone; no link dependency on GoogleTest | Done | haiku | EconomyTestHelpers.h: AssertPoolValue, EventCapture, AssertEventFired. No gtest dep. |
| 9 | GoogleTest suite — exhaustive coverage: schema load + validation errors, pool clamping at min/max, all TransactionResult variants, observer event payloads, modifier stack (simple + conditional), derived resource registration, income accumulation across variable deltas, Transfer atomicity (deduct then deposit), multi-instance isolation | All tests green via `dia run googletest --filter="DiaEconomy*"` | Done | sonnet | One test file per feature area. Use test utilities from Task 8.; 51 tests green. Added LoadFromJsonValue to EconomySchema; fixed copy ctor/operator= for empty DynamicArrays. |
| 10 | E2E CluicheTest stage — `EconomyTestStage`: two instances (PlayerEconomy + AIEconomy), tick loop running 10 sim-seconds, Earn/Spend/Transfer transactions, modifier activation mid-run, pytest scenario checkpoints verifying pool values and event counts | `dia run cluichetest` stage completes all checkpoints; pytest scenario passes | Done | sonnet | Wires `EconomySystem::Tick()` into a SimPU module — exemplar for game code. Console output shows all instance states each second.; E2E stage: 2 gatherers mine + deposit gold, treasury bar, consumer drain, market modifier toggle. EconomyDrawer in Drawers/. EconomyInstance copy fixed. CluicheTest linked against DiaEconomy. |
| 11 | Observability pass — `DIA_LOG_INFO` on schema load, instance create, clamped transactions; `DIA_LOG_WARN` on schema validation warnings + unknown derived resource; `DIA_TRACE_ZONE` on `Tick`; `DIA_PROFILE_SCOPE` on modifier evaluation loop; `MetricRegistry` registrations for pool values + per-instance income rate; `IHealthReporter` for instance coherence (min ≤ value ≤ max invariant) | Log lines appear at correct call sites; no missing instrumentation on any code path added in Tasks 2–10 | Done | sonnet | Full OO scan against all 5 DiaObservation pillars. Run after Task 10 so all code paths exist.; Observability pass: DIA_TRACE_ZONE on Tick, DIA_PROFILE_SCOPE on modifier loop, DIA_LOG_INFO on clamped Earn/Spend + LoadFromJsonValue, EconomyInstanceHealthReporter (IHealthReporter). 71 tests green. |
