**Spec:** @docs/specs/applications/dia/systems/diaaiinspector/diaaiinspector.md
**Status:** In Progress

| # | Task | Test | Status | Model | Notes |
|---|------|------|--------|-------|-------|
| 1 | Add `RuleSet::GetLastFireReport()` to DiaRules (`#ifdef DIA_DEBUG`) | `dia run googletest --filter="RuleSet*"` still passes | Pending | sonnet | Prerequisite for RulesInspectorSource and RulesVisualDebugger. Adds debug-only fire report to RuleSet.h/.cpp. |
| 2 | Editor plugin scaffold — `Dia/DiaAIInspector/` vcxproj, module doc, `DiaAIInspectorPlugin.h/.cpp`, 4 controller headers+cpps, register in Cluiche.sln under `4.0-Editor` | Build compiles clean | Pending | sonnet | Template: DiaBlackboardInspector.vcxproj. Four controllers: AIBudgetController, UtilityAIController, RulesController, HTNController — each thin-forwarding to WebUIBridge. |
| 3 | `AIBudgetInspectorSource` — PeriodicSourceBase (1 Hz), 60-frame rolling history ring buffer | Build + visual inspection of JSON payload structure | Pending | sonnet | Needs `AIBudgetModule::GetLastResult()` accessor (or equivalent) — add to AIBudgetModule if absent. PU boundary: AIBudgetModule lives on SimPU; investigate whether direct read is safe or requires cross-PU stream. |
| 4 | `UtilityAIInspectorSource` — ChangeDetectedSourceBase, iterate entities for UtilitySetComponent, per-action scores via GetLastFrameScores() (DIA_DEBUG guard) | Build + verify DIA_DEBUG/Release conditional compiles | Pending | sonnet | IEntityInspectable has no component-type filter — iterate all, check GetComponentTypeIds() for "utility-set-component". GetLastFrameScores() accessed via component->GetUtilitySet()->GetLastFrameScores(). |
| 5 | `RulesInspectorSource` — ChangeDetectedSourceBase, iterate entities for RuleSetComponent, GetLastFireReport() under DIA_DEBUG | Build + verify conditional compile | Pending | sonnet | Depends on Task 1. Wraps CollectAndHash body in `#ifdef DIA_DEBUG` (SD-003). Component type ID: "rule-set-component". |
| 6 | `HTNInspectorSource` — ChangeDetectedSourceBase, HTNPlannerComponent, server-side plan history ring buffer (depth 8 per entity) | Build + verify plan history JSON emitted correctly | Pending | sonnet | Most complex source. Per-entity ring buffer in .cpp using STL (PD-004 allows STL in .cpp internals). Hash on operator sequence to detect plan replacement. Reason tag: "initial" / "diverged" (HasDiverged was true on prior tick) / "replan". |
| 7 | Wire all 4 sources into DebugServerHostModule — expand kSourceCount, instantiate and activate | `dia run cluichetest` connects and editor receives all 4 topics | Pending | sonnet | Expand fixed array from 5 to 9. Resolve PU boundary: entity-domain sources (UtilityAI, Rules, HTN) access SimPU components — investigate if direct reference is safe or requires cross-PU stream (follow EntityInspectorModule pattern if needed). AIBudgetModule may also need SimPU→MainPU bridging. |
