**Spec:** @docs/specs/applications/dia/systems/diaaiinspector/diaaiinspector.md
**Status:** In Progress

| # | Task | Test | Status | Model | Notes |
|---|------|------|--------|-------|-------|
| 1 | Add `RuleSet::GetLastFireReport()` to DiaRules (`#ifdef DIA_DEBUG`) | `dia run googletest --filter="RuleSet*"` still passes | Done | sonnet | Prerequisite for RulesInspectorSource and RulesVisualDebugger. Adds debug-only fire report to RuleSet.h/.cpp. |
| 2 | Editor plugin scaffold — `Dia/DiaAIInspector/` vcxproj, module doc, `DiaAIInspectorPlugin.h/.cpp`, 4 controller headers+cpps, register in Cluiche.sln under `4.0-Editor` | Build compiles clean | Done | sonnet | Template: DiaBlackboardInspector.vcxproj. Four controllers: AIBudgetController, UtilityAIController, RulesController, HTNController — each thin-forwarding to WebUIBridge. |
| 3 | `AIBudgetInspectorSource` — `AIInspectorModule` (SimPU, DIA_DEBUG), EventStreamWriter<AIInspectEvent>, PushBudget() at 1 Hz with 60-frame rolling history | 7265 tests pass | Done | sonnet | PU boundary resolved: AIInspectorModule on SimPU (mirrors EntityInspectorModule pattern). AIBudgetModule::GetLastResult() + GetBudgetMs() added. AIInspectEvent type + DIA_STREAM_TYPE registered. |
| 4 | `UtilityAIInspectorSource` — PushUtilityAI() inside AIInspectorModule, change-detected via hash, iterates Domain::GetComponent<UtilitySetComponent>(), emits per-entity action scores | Build passes with Task 3 | Done | sonnet | GetLastFrameScores() called via GetUtilitySet(). CluicheGameBaseline.vcxproj gets DiaAIBudget + DiaUtilityAI ProjectReferences. |
| 5 | `PushRules()` inside AIInspectorModule — change-detected, RuleSetComponent, GetLastFireReport() under DIA_DEBUG | Build passes | Done | sonnet | #ifdef DIA_DEBUG guard on fire report body; Release sends empty entities array. DiaRules ProjectReference added to CluicheGameBaseline.vcxproj. |
| 6 | `PushHTN()` inside AIInspectorModule — change-detected, HTNPlannerComponent, server-side plan history ring buffer (depth 8 per entity) in .cpp STL map | Build passes | Done | sonnet | HasDiverged() requires IConditionContext — diverged emitted as false (TODO). Plan hash = task_count ^ currentOpCRC. DiaHTN ProjectReference added. |
| 7 | Wire all 4 sources into DebugServerHostModule — expand kSourceCount, instantiate and activate | `dia run cluichetest` connects and editor receives all 4 topics | Pending | sonnet | Expand fixed array from 5 to 9. Resolve PU boundary: entity-domain sources (UtilityAI, Rules, HTN) access SimPU components — investigate if direct reference is safe or requires cross-PU stream (follow EntityInspectorModule pattern if needed). AIBudgetModule may also need SimPU→MainPU bridging. |
