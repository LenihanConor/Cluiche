# Refactor Plan — Architecture Layer Violation Remediation

**Spec:** none (tech-debt remediation across existing modules, not new feature work)
**Status:** Done — `dia check arch` went from 172 violations at baseline to 0. One unrelated, non-violation-count vcxproj cleanup (task 9) remains Blocked and is tracked as its own Loose Ends entry.
**Scope:** 6 independent violation clusters surfaced by `dia check arch` (the user-supplied error list was a curated excerpt, not the full output), expanded to a 7th metadata-only sweep (task 12) once the original 6 were resolved and 93 `dia.root`-misattribution violations remained across 10 more modules.

## Session Notes — Audit Summary

`dia check arch`'s full output (172 violations at baseline, confirmed via `git stash`/re-run) spans many more root causes than the 6 clusters in the original curated error list this plan was scoped to. One cluster (VisualDebugger family → `DiaVisualDebugger`) was already a documented loose end in `docs/BACKLOG.md` ("DiaVisualDebugger layer tag conflict") calling for "a proper audit" before deciding a fix — this plan is that audit, plus audits of the other 5 clusters found in the same excerpt. Tasks 1–8 and 11 are landed; current count is 93 (net -79, no regressions, confirmed via baseline re-run + full GoogleTest suite). Two clusters (B, F) turned out to be simple mistags rather than real boundary problems — worth checking "is the layer tag just wrong" before reaching for interface extraction, since it's cheaper when it applies. Two real compile errors (not visible to `dia check arch`, only a full build) were caught along the way — see tasks 7 and 8's notes — reinforcing that this plan's Test column always needs an actual build, not just the arch check.

Violations are derived purely from real `#include` paths matched against each module's `path:` in its `dia.*.architecture.module.md` frontmatter, compared via `layer:` tags (`Dia/DiaCLI/dia_cli/commands/check/arch_checker.py` + `arch_module_map.py` + `arch_include_parser.py` + `arch_layer_rules.py`). The `dependent_modules` list is decorative and never consulted, and **there is no allowlist/exception mechanism** — a "sanctioned" cross-dependency (e.g. binding decision SD-002) must be resolved by making the physical layer tags/module boundaries agree, not by declaring an exception.

**Cluster D — RigidBody2DVisualDebugger → `DiaGraphics/Misc/RGBA.h` (trivial):** the codebase already has a foundation-tier `DiaCore/Colour/RGBA.h` used elsewhere (`DebugGroupAccents.h`, `IDebugDomain.h`) instead of the domain-tagged `DiaGraphics/Misc/RGBA.h` — looks like a leftover duplicate type. Fix: repoint the one include, after confirming field-compatibility.

**Cluster C — GridVisibilityVisualDebugger → DiaGridVisibility + DiaPathfinding (trivial):** every other `Dia<X>VisualDebugger` is tagged `domain/<X's own domain>/tools` (e.g. Animation2D → `domain/animation/tools`, SoftBody2D → `domain/physics/tools`). `DiaGridVisibilityVisualDebugger` is the outlier, mistagged `domain/visual/tools` instead of `domain/gameplay/tools` (matching `DiaGridVisibility`/`DiaPathfinding`, both `domain/gameplay/core`). Its module.md's own attempted self-documented exception is also broken (malformed `dia.dia*`-prefixed ids, an inline comment baked into a forbidden-list value) — a no-op today regardless. Fix: retag + fix the malformed ids.

**Cluster E — `dia.root` false attribution, DiaSensor + DiaTriggerScript (metadata-only):** both modules have their own module.md, but neither declares a `path:` key, so the checker never recognizes their directories as their own module and sweeps their files into `dia.root` (`foundation/core`, the most restrictive layer). `DiaTriggerScript` also has a duplicate `module:`/`id:` key collision that mangles its module id, and both use non-standard `layer:` strings and dependency-list shapes the parser doesn't recognize. Once corrected, their real dependencies are all downward from a `domain/gameplay/core`-tier module — this is a pure doc-metadata bug, not a real layering violation.

**Cluster F — DiaSimTime → DiaSaveGame (done — turned out to be a mistag, not a boundary problem):** Original hypothesis was that `dia.savegame` (`domain/gameplay/core`) needed an `IDebugContext`-style interface extraction, since `dia.simtime` (`foundation/application`) only needs `ISaveable`/`SaveContext`/`LoadContext`. Built that extraction (moved the 3 headers + added an `ISaveRegistry` interface for the `SaveRegistry*` DiaSimTimeModule also holds) — but a simpler question surfaced first: is `dia.savegame` even correctly tagged `domain/gameplay/core`? Its own module.md already documented "no domain imports" and forbade `dia.entity`/`dia.economy`/`dia.applicationflow`/`dia.blackboard` as dependencies; its only real deps (`dia.core`, `dia.serializer`) are both foundation-tier; and only `DiaSimTimeModule` + tests depend on it externally (no game/app code yet). Same mistag shape as cluster B's `DiaEditor`. **Retagging `dia.savegame` to `foundation/services` alone fixed all 4 sub-violations**, so the extraction work was reverted (moved the 5 files back, deleted `ISaveRegistry.h`, restored all includes/vcxproj entries) to keep the diff minimal — confirmed via `git status`/`git diff` that the working tree matches HEAD except for the two `layer:`/schema doc edits. Verified: 119 → 115 violations, no new ones.

**Cluster A — VisualDebugger family → `dia.debug.visualdebugger` (largest, the pre-existing Loose Ends item):** hits Animation2D/IK2D/Rig2D/SoftBody2D/AssetRuntime visual debuggers (per the existing BACKLOG note, ~42 files total across every domain visual debugger). Three headers involved: `Domain/IDebugDomain.h` and `Domain/DebugGroupAccents.h/.cpp` are graphics-free (only StringCRC/RGBA/json + a forward decl); `DebugLayerManager.h/.cpp` is not — it stores/returns `Camera2D`/`ViewportTransform`/`Camera3D` by value and touches `DebugFrameDataVisitor`, a real `DiaGraphics`/`DiaGraphics3D` dependency. `DiaDebugDraw` (the abandoned R6 split target from the `layer-formalisation` spec) already exists, tagged `foundation/services`, and already duplicate-compiles `DebugLayerManager` et al. via relative path out of `DiaVisualDebugger` — but its own module.md still lists `dia.graphics` as required, which is itself contradictory for a `foundation/services` tag (the exact tension the BACKLOG entry flagged as unresolved). `IDebugDomain`/`DebugGroupAccents` are not currently part of `DiaDebugDraw`'s compiled set at all. Fix: (1) move the two graphics-free headers into `DiaDebugDraw` now; (2) audit call sites and decompose `DebugLayerManager` into a graphics-free `DebugLayerRegistry` (moves to `DiaDebugDraw`) vs. camera/viewport glue (stays in `DiaVisualDebugger`), following the `IDebugContext` decoupling pattern for any camera/viewport data domain debuggers still need; (3) remove the duplicate-compile vcxproj hack; (4) reconcile the `layer-formalisation` R6 task + SD-002 wording once tags and code agree.

**Cluster B — DiaScalarFieldInspector → DiaEditor (done, retag landed; one follow-up spawned):** `DiaEditor` was tagged `domain/visual/core` but is actually a cross-cutting editor-plugin host framework, not visual-domain content. Confirmed its 5 required deps (`dia.json`, `dia.observation`, `dia.debugprotocol`, `dia.game`, `dia.protobuf`) are all foundation-tier, and its 13 known dependents are all `domain/*/tools` (downward deps, always allowed) — so retagging `dia.editor` to `foundation/application` was safe and landed (task 6). It fixed the `DiaScalarFieldInspector` violation, but re-running `dia check arch` surfaced a **new** violation the pre-check research missed (it only checked module.md-declared deps, not DiaEditor's own actual `#include`s): `Dia/DiaEditor/UI/WebUIBridge.cpp` includes `<DiaUI/IUISystem.h>` (`domain/visual/core`), an undeclared dependency that was fine when DiaEditor was itself `domain/visual/core` (same-domain/same-tier) but is now an upward violation. It's contained to one file — `WebUIBridge` only calls `RegisterJSHandler`/`CallJSFunction` on `IUISystem*`, both already separable JS-bridge concerns on that interface. User decision: keep the retag (net violation count unchanged — one violation swapped for another, but the original inter-domain mistag is fixed) and track the `DiaEditor`→`DiaUI` fix as its own task (11) rather than fix inline, since it needs its own small interface-extraction design (mirrors the `IDebugContext`/cluster F precedent: pull a narrow `IJSBridge` interface — just `RegisterJSHandler`/`CallJSFunction`/the `JSHandler` typedef — into a foundation-tier home, make `IUISystem` inherit it, repoint `WebUIBridge` to depend on the narrow interface instead of `IUISystem` directly).

## Tasks

| # | Task | Test | Status | Model | Notes |
|---|------|------|--------|-------|-------|
| 1 | Cluster D: confirm `DiaCore/Colour/RGBA.h` and `DiaGraphics/Misc/RGBA.h` are field-compatible, then repoint `Dia/DiaRigidBody2DVisualDebugger/PhysicsShapesDrawer.cpp:18` to the DiaCore header. | `dia check arch` clean for this file; `dia run googletest --filter=*RigidBody2D*` | Done | sonnet | `DiaGraphics/Misc/RGBA.h` confirmed to be a deliberate `using RGBA = Dia::Core::RGBA;` compatibility shim (comment in its `.cpp` says so), used by 25 other visual-domain files — left intact, only this one cross-domain include was repointed. Verified clean via `dia check arch`. |
| 2 | Cluster C: retag `dia.gridvisibilityvisualdebugger` to `domain/gameplay/tools`; fix malformed `dia.dia*` ids and the inline-comment-in-value in `dependent_modules`/`forbidden`. | `dia check arch` clean for this module | Done (partial — see note) | sonnet | Retag + id fixes landed (original 3 violations against DiaGridVisibility/DiaPathfinding gone; the self-contradictory `forbidden: dia.gridvisibility` entry removed, matches sibling debugger modules that have no forbidden list either). But `GridVisibilityDebugDomain.h` also includes the same 3 `DiaVisualDebugger` headers as cluster A — those were silently allowed at `domain/visual/tools` (same-domain) and are now newly flagged at `domain/gameplay/tools` (cross-domain). This module is actually part of cluster A too; folded into tasks 7/8's scope (add it to the "5+ affected debugger modules" list). Net violation count for this file unchanged until cluster A lands. |
| 3 | Cluster E: rewrite DiaSensor + DiaTriggerScript module.md to standard schema — add `path:`, single `module_id:` key, `dependencies.required`/`forbidden` lists, `layer: domain/gameplay/core`. Confirm layer group choice against sibling AI/gameplay modules first. | `dia check arch` — files no longer attributed to `dia.root`, no new violations | Done | sonnet | Both normalized to `layer: domain/gameplay/core`, matching DiaGridVisibility/DiaPathfinding/DiaSensor siblings. DiaTriggerScript's id fixed from malformed `DiaTriggerScript` to `dia.triggerscript` (schema also had non-standard `module:`/`id:`/`namespace:` keys, now standard). Verified: neither module's files attributed to `dia.root` anymore, no new violations. **Found the same bug pattern in `DiaEntitySpatial`** (no `path:`, `layer: domain-adapter`, flat `dependencies:` list) and **`DiaSchemaBrowser`** (no module.md at all) while investigating — not fixed, flagged as new follow-up scope. |
| 4 | Cluster F: normalize `dia.simtime` module.md to `required:`/`forbidden:` schema (currently a non-standard flat list). | `dia docs registry` regen clean | Done | haiku | Also fixed non-standard `display_name`/`parent_module`/`not_responsibilities` keys to match schema. |
| 5 | Cluster F: retag `dia.savegame` from `domain/gameplay/core` to `foundation/services` (its own docs already claimed "no domain imports"; only real deps are foundation-tier `dia.core`/`dia.serializer`). | `dia check arch` clean; `dia run googletest --filter=*SaveGame*:*SimTime*` | Done | opus | Started as an `IDebugContext`-style interface extraction (moved `ISaveable`/`SaveContext`/`LoadContext` to `DiaCore/Save/`, added `ISaveRegistry` for the `SaveRegistry*` DiaSimTimeModule holds) — reverted once the simpler retag was confirmed sufficient, per user decision to keep the diff minimal. All 4 sub-violations fixed, no new ones. |
| 6 | Cluster B: spot-check `dia.debugprotocol` and `dia.protobuf` layer tags are foundation-tier, then retag `dia.editor` from `domain/visual/core` to `foundation/application`. **Gated on user sign-off before starting.** | `dia check arch` clean for DiaScalarFieldInspector; no new violations from other DiaEditor dependents | Done | opus | User sign-off given. Retag landed; fixed the ScalarFieldInspector violation but surfaced a new DiaEditor→DiaUI one — tracked as task 11 per user decision (keep retag, don't fix inline). |
| 7 | Cluster A step 1: move `Domain/IDebugDomain.h` and `Domain/DebugGroupAccents.h/.cpp` from `DiaVisualDebugger` into `DiaDebugDraw`; repoint the affected debugger modules' includes (Animation2D, IK2D, Rig2D, SoftBody2D, AssetRuntime, **and GridVisibility per task 2's note**). | `dia check arch` — these 2 headers' violations gone; full googletest pass | Done | sonnet | Actually ~104 files reference these 2 headers (every domain debug-domain module, not just the 6 named) — bulk repoint via sed across `.h`/`.cpp`/spec docs. `DiaVisualDebugger.vcxproj.filters` turned out to already have pre-existing broken nested-tag structure (unrelated debt); removed the 2 files' entries carefully without disturbing the rest, verified still-valid XML before/after. New violation surfaced: `dia.debugdraw` (now containing real files for the first time) → `dia.core.debugdraw`, a genuine foundation sub-level-ordering violation (services can't depend on platform) that only existed latently before since `dia.debugdraw` had zero checker-attributed files pre-move. Tried retagging `dia.core.debugdraw` to services first (matches its own summary text, which already contradicted its `platform` tag) — made things worse (violates against its own `dia.maths` dependency, services and maths are the same sub-level/different group). Fix that worked: retag `dia.debugdraw` itself to `foundation/platform` (matches `dia.core.debugdraw`'s real tier, both need DiaMaths transitively) — net -1, no side effects. Full-suite build caught a **real compile error** `dia check arch` couldn't see: `EditorView.cpp`'s `new WebUIBridge(uiSystem)` (task 11) needs `IUISystem`'s complete type to upcast to `IJSBridge*`, but `EditorView.cpp` only had a forward declaration — adding the full `#include` would have reintroduced the task-11 violation, so instead `EditorView::Initialize` now takes the already-upcast `IJSBridge*` as a second parameter, cast by its one caller (`Cluiche/CluicheEditor/.../EditorViewModule.cpp`, application-layer, unrestricted). 114 → 100; full GoogleTest suite (not just a filter) passed after the fix. |
| 8 | Cluster A step 2: audit each affected debugger's actual `DebugLayerManager` call sites; decompose it into a graphics-free `DebugLayerRegistry` (moves to `DiaDebugDraw`) and camera/viewport/`DebugFrameDataVisitor` glue (stays in `DiaVisualDebugger`); repoint debuggers to the registry only, exposing any still-needed camera/viewport data via a small interface (`IDebugContext`-style) rather than the concrete class. | `dia check arch` clean for all affected debuggers; full googletest pass | Done | opus | Turned out narrower than feared: audited actual call sites first (not a blind sweep) — the 7 originally-violating modules call only `Register`/`Unregister`; the full 18-module family (done at user's request, for consistency, not just the 7) also uses `EnableLayer`/`DisableLayer`/`IsLayerEnabled`/`SetDebugScale` — all confirmed graphics-free in `DebugLayerManager.cpp`'s actual implementation (a `LayerEntry{debugger,priority,stageTag,active}` array, zero graphics types touched). New `IDebugLayerRegistry` (extends the existing `IDebugContext`, mirrors its precedent) added to `DiaDebugDraw`; `DebugLayerManager` implements it as an added base (zero logic moved/duplicated); `IDebugDomain::Register/Unregister` signature changed to the interface type; all 18 modules' overrides follow (bodies unchanged — they only ever called interface methods). The one real call site constructing a concrete `DebugLayerManager` (`Cluiche/CluicheGameBaseline/Modules/VisualDebuggerModule.cpp`, application-layer) needed no change — implicit upcast, same as task 7's `EditorViewModule.cpp` lesson. **Real compile error caught by full build** (not `dia check arch`): `Coord2DDebugDomain`/`Coord3DDebugDomain` — excluded from the sweep as same-domain — also override `IDebugDomain::Register` and no longer matched the new base signature. They genuinely need the concrete `DebugLayerManager` (`GetCursorWorld`, `GetCamera3D`, `RegisterWithoutDraw` — none in the graphics-free interface), so fixed by taking `IDebugLayerRegistry&` to satisfy the override and `static_cast`ing back to `DebugLayerManager&` internally (safe — it's the only implementor; RTTI is disabled repo-wide so `dynamic_cast` wasn't an option anyway). Also removed 4 dead `#include <DiaVisualDebugger/DebugLayerManager.h>` lines in `DiaGeometry2DVisualDebugger/*.inl` that were never actually used. 100 → 93 violations; full GoogleTest suite passes. |
| 9 | Cluster A step 3: remove the duplicate-compile relative-path hack in `DiaDebugDraw.vcxproj`; update `layer-formalisation` spec's R6 task status and reconcile with `DiaDebugDomain`'s SD-002 wording. | `dia check sln-sync`; build clean | **Blocked** | sonnet | Tried it: removing the 8 duplicate-compiled files (keeping only the genuinely-owned `Domain/*` entries) broke the real link — `GoogleTests.exe` failed with 59 unresolved externals (`DebugLayerManager::RegisterWithoutDraw`, `GetCamera3D`, `DebugColourPalette::kFlowDirection`, etc.). Confusingly, `DiaDebugDraw.lib` isn't referenced anywhere in `GoogleTests.vcxproj` (not in `ProjectReference`, not in `AdditionalDependencies`) while `DiaVisualDebugger.lib` — which should contain these exact symbols, since `DebugLayerManager.cpp` lives there — is explicitly linked. Reverted immediately (confirmed clean via `git status` + a passing targeted test run); doesn't affect the violation count either way (pure vcxproj hygiene). Working theory, unconfirmed: a shared intermediate-object-path collision between the two projects compiling the same source files under MSBuild's default parallel build. Needs real investigation (check `IntDir` settings, try a forced serial rebuild) before attempting again — do not retry without that. |
| 10 | Update `docs/BACKLOG.md`: replace the "DiaVisualDebugger layer tag conflict" Loose Ends row with a pointer to this plan. | `git status` clean | Done | haiku | Superseded by the actual outcome: since `dia check arch` reached 0 violations, used `dia docs backlog move` to move the whole entry to `BACKLOG-HISTORY.md` instead (with notes on the 172→0 result and task 9's separate Blocked status), and added a new, smaller Loose Ends row specifically for task 9 (the still-outstanding vcxproj duplicate-compile hack). |
| 11 | Cluster B follow-up: extract a narrow `IJSBridge` interface (`RegisterJSHandler`/`CallJSFunction`/`JSHandler` typedef) into a foundation-tier home; make `IUISystem` inherit it; repoint `WebUIBridge` to depend on `IJSBridge` instead of `IUISystem` directly. | `dia check arch` clean for `dia.editor`; `dia run googletest --filter=*Editor*:*UI*` | Done | sonnet | Checked first whether `dia.ui` was simply mistagged (per the F lesson) — it isn't: retagging it to `foundation/platform` in a throwaway test made things worse (new same-sublevel-different-group violation both ways, plus `DiaUI` already depends on `DiaApplicationFlow` itself). Real fix: new `Dia::Core::IJSBridge` in `Dia/DiaCore/UI/` (module `dia.core.ui`, foundation/core); `IUISystem` now inherits it and no longer redeclares `RegisterJSHandler`/`CallJSFunction`; `WebUIBridge` takes `IJSBridge*` instead of `IUISystem*`. `EditorView.cpp`'s construction call site needed no change (implicit upcast). 115 → 114, no new violations. |
| 12 | New follow-up (found during task 3, expanded after tasks 7/8 fully resolved the original 6 clusters — user asked for the remaining `dia.root` violations to be swept too): normalize module.md for all 10 modules whose files were being misattributed to `dia.root` (`DiaBehaviourTreeVisualDebugger`, `DiaEntitySpatial`, `DiaEntitySpatialVisualDebugger`, `DiaEntitySpawner`, `DiaFlowFieldVisualDebugger`, `DiaHTNVisualDebugger`, `DiaMailboxVisualDebugger`, `DiaObjective`, `DiaScalarFieldVisualDebugger`, `DiaSchemaBrowser`) — same metadata-bug shape as cluster E (missing `path:`, or in 3 cases no module.md at all). | `dia check arch` — 0 violations; full googletest pass | Done | sonnet | 6 had a module.md but were missing `path:`/used non-standard schema (same fix pattern as task 3); 3 had no module.md at all (`DiaEntitySpatialVisualDebugger`, `DiaScalarFieldVisualDebugger`, `DiaSchemaBrowser` — created fresh, matching sibling conventions: `domain/gameplay/tools` for world-drawer debuggers already using `IDebugLayerRegistry`, `domain/visual/tools` for panel-only/editor-plugin ones matching `DiaEntityInspector`/`DiaAIInspector` precedent). One early theory (a misplaced doc) turned out wrong on closer read — `Dia/DiaEntitySpatial/Docs/dia.diaentityspatialvisualdebugger...md` is a real, correctly-placed doc for an unrelated header-only `DiaEntitySpatial/Adaptors` module that just has a confusingly similar name; left untouched. Registered the 3 new docs in their vcxproj/filters as `None` items (first attempt via `dia docs vcxproj-add` incorrectly typed them as `ClCompile` — a `.md` file would never compile; fixed manually, one filters-file XML mismatch caught and corrected before it could break anything). **`dia check arch` now reports 0 violations — the full 172-violation baseline is resolved.** Full GoogleTest suite passes. |

All tasks landed except task 9 (Blocked, tracked separately). Final order taken: 1, 2, 3, 4, 5, 6, 11, 7, 8, 12, 10 — 9 attempted and reverted. Commit after each task per CLAUDE.md workflow (7 commits total: `50a1f7c4`, `656222c7`, `529e4049`, `f043bcfc`, `c5a3d728`, plus task 12/10's commit).

## Acceptance

- AC1 (D): `PhysicsShapesDrawer.cpp` no longer includes `DiaGraphics/Misc/RGBA.h`; no arch violation; RigidBody2D debugger tests pass. ✅
- AC2 (C): `dia.gridvisibilityvisualdebugger` layer tag matches its debugged domain; no arch violation. ✅
- AC3 (E): DiaSensor + DiaTriggerScript files attributed to their own modules, not `dia.root`; no new violations introduced by correct attribution. ✅
- AC4 (F): `dia.savegame` tagged `foundation/services`; `DiaSimTime`→`DiaSaveGame` no longer flagged; SimTime + SaveGame tests pass. ✅
- AC5 (A): none of the affected domain visual debuggers include anything from `DiaVisualDebugger` at `domain/visual/tools`; full GoogleTest suite green. ✅ (partial: `DiaDebugDraw.vcxproj` still duplicate-compiles rather than owning its files outright — task 9, Blocked, tracked as its own Loose Ends entry since it's unrelated to the violation count.)
- AC6 (B): `DiaScalarFieldInspector` → `DiaEditor` no longer flagged; no new violations from other `DiaEditor` dependents. ✅
- AC7: `dia check arch` reports zero violations. ✅ **Confirmed: `Total: 0 violations — architecture is clean`.** `docs/BACKLOG.md` updated — the whole remediation entry moved to `BACKLOG-HISTORY.md`; task 9 tracked as a new, separate Loose Ends row.
